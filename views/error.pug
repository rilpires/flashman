extends layout

block content
  .row.mt-2.justify-content-center
    if message
      h1 #{message}
    if status
      h2 #{status}
    if stack
      pre #{stack}

block scripts
  script(src='/dist/error.bundle.js')
